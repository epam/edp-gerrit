apiVersion: v1
kind: Service
metadata:
  name: {{ include "edp-gerrit.fullname" . }}
  labels:
    {{- include "edp-gerrit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edp-gerrit.selectorLabels" . | nindent 4 }}
